<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>LOL MATCH</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700,900&display=swap" rel="stylesheet">
  </head>

  <body>
<header class="header">
<div class="scrim">SCRIM PARTNER</div>
 <% if user_signed_in? %>
  <div class="nav__right">
   <%= link_to "トップページ", root_path, class: :nav__logout %> 
   <%= link_to "パートナー管理", relationship_path(current_user.id), class: :nav__logout%>
   <%= link_to "チーム編集", edit_user_path(current_user), class: :nav__logout %>
   <%= link_to "ログアウト", destroy_user_session_path, class: :nav__logout %>
  </div> 
 <% else %>
  <div class="nav__right">
   <%= link_to "トップページ", root_path, class: :nav__logout %> 
   <%= link_to "ログイン", new_user_session_path, class: :nav__btn %>
   <%= link_to "新規登録", new_user_registration_path, class: :nav__btn %>
  </div>
 <% end %> 
</header>
    <%= yield %>
  </body>
</html>