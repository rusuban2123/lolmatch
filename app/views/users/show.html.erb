
<div class="chat">
<% if @user.id == current_user.id %>
<% else %>
<META http-equiv="Refresh" content="0.001;URL=/rooms/<%= @roomId %>">
  <% if @isRoom == true %>
  <div class="startchat">
    <p><a href="/rooms/<%= @roomId %>"></a>
  </div>
  <% else %>
    <%= form_for @room do |f| %>
      <%= fields_for @entry do |e| %>
        <%= e.hidden_field :user_id, :value=> @user.id %>
      <% end %>
   <%= f.submit "" %>
    <% end %>
  <% end %>
<% end %>
  <div class="gotop">
   <%= link_to "", root_path %>
  </div>
</div>